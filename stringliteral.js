const frost =
	"Nature’s first green is gold, \
Her hardest hue to hold. \
Her early leaf’s a flower; \
But only so an hour. \
Then leaf subsides to leaf. \
So Eden sank to grief, \
So dawn goes down to day. \
Nothing gold can stay.";

console.log(frost);

const poem =
	"\tNature’s first green is gold, \nHer hardest hue to hold. \nHer early leaf’s a flower; \nBut only so an hour. \nThen leaf subsides to leaf. \nSo Eden sank to grief, \nSo dawn goes down to day. \nNothing gold can stay.";

    console.log(poem);

    console.log('They said, "this string doesn\'t need an escape," but that isn\'t true.');

    const poemUpdated =
	"Nature’s first green is gold, \n\
Her hardest hue to hold. \n\
Her early leaf’s a flower; \n\
But only so an hour. \n\
Then leaf subsides to leaf. \n\
So Eden sank to grief, \n\
So dawn goes down to day. \n\
Nothing gold can stay.";

console.log(poemUpdated);

const templateLiteralPoem =
`Nature’s first green is gold,
Her hardest hue to hold.
Her early leaf’s a flower;
But only so an hour.
Then leaf subsides to leaf.
So Eden sank to grief,
So dawn goes down to day.
Nothing gold can stay.`;

console.log(templateLiteralPoem);

let userName ="Jackie";
let message ="How are you doing today?"
console.log(`Good Morning ${userName} find your money ${400+200}$. ${message}`);

const a = 5;
const b = 10;

console.log(`'a' is assigned a value of "${a}," and 'b' is assigned a value of "${b}."
Therefore, 'a' plus 'b' is equal to ${a + b}.`);

console.log(typeof(null))
console.log("10"+5)
console.log(1+2===3)
/*hkjh
hgjg
*/